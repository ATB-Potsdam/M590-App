# Release notes — 0.1.42 (versionCode 41)

Bundle: `app-release.aab` (34 MB), signed with the `upload` key
(SHA-256 `93:61:DB:E4:59:99:90:32:05:67:72:EB:10:1F:C9:2F:AB:20:86:70:EC:75:C1:74:2C:59:BA:04:18:1A:98:8F`).

Previous Play release: 0.1.41 (versionCode 37).

Every release build raises versionCode (see `scripts/buildAndroid.sh`), so a
rebuild of this same version produces 40, 41, … — check the build output rather
than trusting the number above if the bundle has been rebuilt since.

---

## Store text

The store text for this release is no longer a separate file: it lives in
`PENDING.txt`, which accumulates the text of every release since the last Play
upload and is pasted whole. See [README.md](README.md).

---

## What is in this build

Two user-facing changes since 0.1.41:

- **Summary totals no longer report mm/a** (`1b5493d`). The project totals divided
  summed m³/a by summed field area, but golf's mm/a is referenced to its irrigated
  sub-areas (greens/tees/fairways, Table 35 — 20.57 ha on a 45 ha course), not to
  the field. Mixing the two denominators let the total come out below every
  individual row. Totals now show m³/a only; per-assignment mm/a stays, with a
  footnote on golf naming its reference area. Applies to the on-screen summary and
  the PDF. Reported by a tester on 2026-08-07.
- **Hint on choosing the nFKWe class for heterogeneous fields** (`e65d667`). The
  class comes from a single map click, so on a field spanning several soil units it
  is close to arbitrary while feeding straight into the water demand. A new
  collapsible in the field form suggests sampling several spots, reading off the
  spread, and then setting a representative class by hand — the tester's own
  proposal. Same feedback thread.

Everything else in this version is developer tooling and does not affect the app:

- `20643a9` build: the Android build reads signing passwords from a gitignored
  `keystore.properties`
- `09a8c0a` chore: internal documentation moved to a private companion repository
- `11a26a0` fix: build scripts locate the JDK/SDK instead of hardcoding paths

## Bundle size

33.0 MB, down from 34.8 MB: the obsolete March–October *precipitation* raster
(`precip_1991-2020_mar_oct.bin`, 1.8 MB compressed) is no longer in
`app/public/data/`. It was left over from before the annual-precipitation fix and
referenced nowhere — the app loads the full-year precipitation raster and the
March–October **ET₀** raster. `scripts/build_all.sh` does not regenerate it
(precip is built with `--months 1-12`).

The remaining bulk is geodata: `nfkwe.fgb` alone is 21.4 MB compressed (61% of the
bundle), plus 2.2 MB for `Klimaraeume.fgb` and 3.6 MB of rasters. Any further
meaningful reduction means simplifying the soil-polygon geometry, which changes
lookup results at polygon edges and would need validating against the sites the
testers already checked. Note the `.aab` is not the user's download size — check
the Play Console's App-size report before optimising further.

## Note on the web release

`dwa.runlevel3.de` was deployed at 0.1.42 *before* the nFKWe hint landed, so the
live web app carries only the summary fix. Re-run `scripts/deploy.sh` to bring the
two in line.

## Checklist before uploading

- [ ] Play Console → Production (or the test track in use) → create release
- [ ] Upload `app-release.aab`
- [ ] Paste the German block above into "What's new in this release"
- [ ] Confirm the shown version reads 0.1.42 (41)
